#
# GD32F303CCT6 (CMSIS-DAP / SWD)
#
# Use GigaDevice OpenOCD (`gd32f30x` flash driver). Do not hardcode
# FLASH_SIZE 0x40000: that driver then treats the part as dual-bank and
# only maps bank0 as 128 KB. FLASH_SIZE 0 probes 0x1FFFF7E0 (0x0100 = 256 KB).
#

adapter driver cmsis-dap
transport select swd

source [find target/swj-dp.tcl]
source [find mem_helper.tcl]

if { [info exists CHIPNAME] } {
   set _CHIPNAME $CHIPNAME
} else {
   set _CHIPNAME gd32f30x
}

set _ENDIAN little

if { [info exists WORKAREASIZE] } {
   set _WORKAREASIZE $WORKAREASIZE
} else {
   set _WORKAREASIZE 0x1000
}

if { [info exists FLASH_SIZE] } {
   set _FLASH_SIZE $FLASH_SIZE
} else {
   set _FLASH_SIZE 0
}

if { [info exists CPUTAPID] } {
   set _CPUTAPID $CPUTAPID
} else {
   set _CPUTAPID 0x2BA01477
}

swj_newdap $_CHIPNAME cpu -irlen 4 -ircapture 0x1 -irmask 0xf -expected-id $_CPUTAPID
dap create $_CHIPNAME.dap -chain-position $_CHIPNAME.cpu

set _TARGETNAME $_CHIPNAME.cpu
target create $_TARGETNAME cortex_m -endian $_ENDIAN -dap $_CHIPNAME.dap

$_TARGETNAME configure -work-area-phys 0x20000000 -work-area-size $_WORKAREASIZE -work-area-backup 0

set _FLASHNAME $_CHIPNAME.flash
flash bank $_FLASHNAME gd32f30x 0x08000000 $_FLASH_SIZE 0 0 $_TARGETNAME

adapter speed 500

reset_config none
if {![using_hla]} {
    cortex_m reset_config sysresetreq
}
